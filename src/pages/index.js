import React from "react"
import Layout from "../components/layout"

const IndexPage = ({ data }) => {
  return (
    <Layout>
      <div>Testing layout insert component.</div>
    </Layout>
  )
}

export default IndexPage
